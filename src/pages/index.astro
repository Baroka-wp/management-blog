---
import Layout from '../layouts/Layout.astro'
import { getCollection } from 'astro:content'
import ArticleCard from '../components/ArticleCard.astro'

const articles = await getCollection('articles')
const derniersArticles = articles.slice(0, 3)

---

<Layout title="Dojo du Management">
  <main class="accueil">
    <section class="hero">
      <h1>Dojo du Management</h1>
      <p>Explorez l'art de diriger avec clarté et sérénité.</p>
      <p><a href="/articles/" class="cta">Voir tous les articles</a></p>
    </section>

    <section class="citation">
      <blockquote>
        <p>« Gérer, c’est savoir écouter le silence entre deux actions. »</p>
        <cite>— Sagesse Zen du Manager</cite>
      </blockquote>
    </section>

    <section class="edito">
      <h2>📜 Lettre du Sensei du Management</h2>
      <p>
        Bienvenue dans notre dojo numérique. Chaque mois, je vous livre une pensée sur l’art de diriger avec clarté et sérénité.
      </p>
      <p>
        <em>“En chaque moment de tension sommeille un apprentissage.”</em>
      </p>
    </section>

    <section class="articles">
      <h2>🗞️ Derniers articles</h2>
      {derniersArticles.map(({ data, slug }) => (
        <ArticleCard
          title={data.title}
          description={data.description}
          pubDate={data.pubDate}
          href={`/articles/${slug}/`}
        />
      ))}
    </section>

  </main>

  <style>
    .accueil {
      max-width: 700px;
      margin: auto;
      padding: 3rem 1rem;
      font-family: 'Noto Serif JP', serif;
    }

    .hero {
      text-align: center;
      margin-bottom: 3rem;
    }
    .hero h1 {
      font-size: 2.2rem;
      margin: 0;
      color: var(--indigo);
      font-family: 'Sawarabi Mincho', serif;
    }
    .hero .cta {
      display: inline-block;
      margin-top: 1rem;
      padding: 0.5rem 1rem;
      background: var(--indigo);
      color: white;
      text-decoration: none;
      border-radius: 4px;
    }
    .hero .cta:hover {
      background: var(--accent);
    }

    .citation blockquote {
      font-style: italic;
      font-size: 1.2rem;
      padding: 1rem;
      margin: 2rem 0;
      border-left: 3px solid var(--accent);
      color: var(--ink);
    }

    .edito {
      background: rgba(255, 255, 255, 0.85);
      padding: 2rem;
      border: 1px solid #ddd;
      margin-bottom: 3rem;
    }

    .articles ul {
      list-style: none;
      padding: 0;
    }

    .articles li {
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 1px dashed #ccc;
    }

    .articles a {
      text-decoration: none;
      color: var(--text);
      transition: color 0.3s;
    }

    .articles a:hover {
      color: var(--accent);
    }

    h2 {
      margin-top: 3rem;
      font-size: 1.6rem;
    }

    small {
      color: #888;
      font-size: 0.85rem;
    }
  </style>
</Layout>
